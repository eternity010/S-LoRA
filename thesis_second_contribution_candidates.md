# 大论文第二大创新点备选方向

> **📅 创建时间**: 2026-04-16
> **📌 背景**: 导师建议开题需要三个点（两个大创新点 + 一个可视化系统），现有工作（RWPT 路由 + 主动复制 + 三维淘汰）打包为第一大创新点，需要确定第二大创新点方向。

---

## 第一大创新点（已完成）

多租户 LoRA 推理的多 Worker 数据并行调度系统：
- RWPT 感知路由（感知层）
- 热门 Adapter 主动复制（执行层）
- 三维评分淘汰机制（保障层）

三者形成"发现瓶颈 → 打破瓶颈 → 解决副作用"的闭环。

---

## 备选方向评估

### 方向 A：预测式 Adapter 预加载与放置

**核心思想**：用请求历史和流量模式预测未来哪些 Adapter 会被访问，提前加载到合适的 Worker，消除冷启动。

**与第一大创新点的区别**：主动复制是响应式的（检测到拥塞才复制），预加载是预测式的（还没拥塞就提前准备）。

**关键问题**：
- 时序预测模型设计（Zipf 分布下的热点漂移检测）
- 预加载时机和目标选择（太早浪费显存，太晚来不及）
- 与现有淘汰机制的协调（预加载的 Adapter 不应被淘汰）

**技术衔接**：已有 preload RPC 通道可复用，ReplicaManager 的 replica_map 可作为输入。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐ (2/5) |
| 论文贡献度 | ⭐⭐⭐ (3/5) |
| 实际需求 | ⭐⭐⭐ (3/5) |
| 实验可行性 | ⭐⭐⭐⭐⭐ (5/5) — 现有 3×3090 环境即可 |

**优势**：实现最简单，和现有代码衔接最紧密。
**风险**：创新深度可能不够"大"，审稿人可能觉得只是工程优化。

---

### 方向 B：QoS 感知的多租户调度

**核心思想**：不同租户有不同的 SLA 等级（金牌 P99<5s，银牌 P99<15s），路由和缓存管理需要优先级感知。

**关键问题**：
- 优先级感知路由（高优先级请求优先选缓存命中的 Worker）
- 抢占式调度（低优先级请求可被中断让路）
- Adapter 缓存优先级分区（高优先级租户的 Adapter 不容易被淘汰）
- 公平性保障（低优先级租户不能被完全饿死）

**技术衔接**：改动集中在 router 和 infer_adapter，都是已经很熟的文件。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐⭐ (2.5/5) |
| 论文贡献度 | ⭐⭐⭐⭐ (4/5) |
| 实际需求 | ⭐⭐⭐⭐⭐ (5/5) — 工业界刚需 |
| 实验可行性 | ⭐⭐⭐⭐ (4/5) — 需要设计多租户混合负载生成器 |

**优势**：motivation 极强，审稿人一看就懂。有理论深度（公平性分析、SLA 保障证明）。
**风险**：需要设计合理的多租户实验场景，实验设计比实现更难。

---

### 方向 C：基于全局感知的 Adapter 动态跨节点放置

**核心思想**：从"局部最优"到"全局最优"。现在每个 Worker 的 Adapter 缓存是自然形成的（请求路由到哪就加载到哪），没有全局最优放置策略。

**关键问题**：
- 全局放置算法（约束优化：最大化全局缓存命中率，约束是每个 Worker 的显存容量）
- 迁移时机决策（太频繁导致抖动，太保守跟不上流量变化）
- 迁移过程中的一致性（迁移中的 Adapter 请求怎么处理）

**技术衔接**：已有 preload RPC 通道、replica_map 全局视图、三维淘汰的 preserve_dirs 保护机制。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐⭐ (3/5) |
| 论文贡献度 | ⭐⭐⭐⭐ (4/5) |
| 实际需求 | ⭐⭐⭐⭐ (4/5) |
| 实验可行性 | ⭐⭐⭐⭐⭐ (5/5) — 现有环境即可 |

**优势**：有理论深度（bin packing 变体），和第一大创新点形成"局部 → 全局"递进，叙事自然。
**风险**：需要证明全局放置比局部贪心有显著收益，如果 Adapter 数量不够多可能差异不明显。

---

### 方向 D：异构 GPU 场景下的自适应调度

**核心思想**：不同型号 GPU（A100 vs 3090 vs T4）混合部署时，让系统感知每个 Worker 的硬件能力差异，自适应调整路由、复制和淘汰策略。

**关键问题**：
- 算力感知路由：弱卡的负载惩罚系数更大（同样的 RWPT，弱卡"实际压力"更高）
- Per-worker 的 Capacity 和 T_congestion
- 副本放置时考虑卡的能力：高 rank 热门 Adapter 优先复制到强卡
- 淘汰阈值按显存大小自适应

**技术衔接**：已有 Worker profiling 机制（`profile_decode_cost_alpha`），可复用来探测算力差异。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐⭐ (2.5/5) |
| 论文贡献度 | ⭐⭐⭐⭐ (4/5) |
| 实际需求 | ⭐⭐⭐⭐⭐ (5/5) — 工业界常态 |
| 实验可行性 | ⭐⭐⭐ (3/5) — 需要异构卡环境，或用 dummy 模式模拟 |

**优势**：motivation 极强（公司不会一次性全换同型号卡），学术界几乎没人做异构 LoRA 推理。
**风险**：实验环境是最大障碍——需要真的有不同型号的卡，或者设计可信的模拟方案。

---

### 方向 E：多机分布式 LoRA 推理

**核心思想**：从单机多卡扩展到多机多卡，涉及跨机器的 Adapter 放置、网络感知路由、故障恢复。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐⭐⭐ (4/5) |
| 论文贡献度 | ⭐⭐⭐⭐⭐ (5/5) |
| 实际需求 | ⭐⭐⭐⭐⭐ (5/5) |
| 实验可行性 | ⭐⭐ (2/5) — 需要多台机器 |

**优势**：贡献度最高，直接对标工业级部署。
**风险**：工程量大，需要多机实验环境，调试分布式系统耗时。

---

### 方向 F：推理与微调共存（Serving-Training Co-location）

**核心思想**：同一 GPU 集群同时跑推理和在线微调，资源动态分配。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐⭐⭐⭐ (4.5/5) |
| 论文贡献度 | ⭐⭐⭐⭐⭐ (5/5) |
| 实际需求 | ⭐⭐⭐⭐⭐ (5/5) |
| 实验可行性 | ⭐⭐ (2/5) — 需要集成训练框架 |

**优势**：问题新颖，实际需求极强。
**风险**：工程量最大，S-LoRA 推理引擎和训练框架是完全独立的代码路径。

---

### 方向 G：LoRA + Speculative Decoding 联合优化

**核心思想**：Speculative Decoding 在 LoRA 场景下的适配，draft model 和 target model 都需要 Adapter。

| 维度 | 评分 |
|------|------|
| 工程难度 | ⭐⭐⭐⭐⭐ (4.5/5) |
| 论文贡献度 | ⭐⭐⭐⭐ (4/5) |
| 实际需求 | ⭐⭐⭐ (3/5) |
| 实验可行性 | ⭐⭐ (2/5) — S-LoRA 不支持 Speculative Decoding |

**优势**：技术前沿。
**风险**：需要从零实现 Speculative Decoding 支持，工程量巨大。

---

## 综合推荐排序

按"投入产出比"（工程可行性 × 论文贡献度 / 工程难度）排序：

| 排名 | 方向 | 推荐理由 |
|------|------|---------|
| 1 | C. 全局 Adapter 放置 | 理论深度好、实验可行、和第一点递进自然 |
| 2 | D. 异构 GPU 调度 | motivation 极强、实现不难，但需要解决实验环境 |
| 3 | B. QoS 多租户 | 工业刚需、有理论深度，实验设计是主要挑战 |
| 4 | A. 预测式预加载 | 最容易做，但创新深度可能不够"大" |
| 5 | E. 多机分布式 | 贡献度最高但工程量和实验环境要求也最高 |
| 6 | F. 推理-微调共存 | 问题好但工程量太大 |
| 7 | G. Speculative Decoding | 需要从零实现太多基础设施 |

---

## 待讨论

- [ ] 和导师确认哪个方向更符合开题要求
- [ ] 确认实验环境（是否有异构卡、是否有多机）
- [ ] 第三个创新点（可视化系统）的范围和技术栈
